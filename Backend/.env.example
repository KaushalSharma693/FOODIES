PORT=5000
# Example .env file for local development — DO NOT commit real secrets
# Replace placeholder values with your environment's secrets when deploying.

MONGO_URI=mongodb://<username>:<password>@host:27017/<dbname>
JWT_SECRET=your_jwt_secret_here

# Firebase config (leave blank here)
FB_API_KEY=
FB_AUTH_DOMAIN=
FB_PROJECT_ID=
FB_APP_ID=
